package com.abhishek.Notes;

public class Detail_Cycle_sort {
    // --> when given numbers from ranges 1 to N => use cycle sort
    // --> how we are running this...?
    //     -> index = value - 1
    //     -> why..? => because index value starts from 0
    // --> complexity = O(N) => linear

    // Tips :-
    //          -> if range => [0 ,N]
    //                      - every element will be at index = value
    //          -> if range => [1, N]
    //                      - every element will be at index = value - 1
}
